{
  "name": "parsedle",
  "version": "0.1.0",
  "private": true,
  "description": "A daily guessing game over your guild's Warcraft Logs parses.",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "build": "node build.mjs",
    "test": "node --test",
    "reports": "node server/cli.js",
    "check": "node server/cli.js check",
    "digest": "node server/cli.js digest --dry-run"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "^2.5.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
